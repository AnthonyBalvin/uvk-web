---
import Layout from '../layouts/Layout.astro';
import { supabase } from '../lib/supabaseClient';

// Obtener productos reales de la base de datos
const { data: alimentos } = await supabase
  .from('alimentos')
  .select('*')
  .order('nombre');
---

<Layout title="Alimentos y Bebidas - UVK Cines">
  <main class="min-h-screen pt-28 pb-16" style="background: linear-gradient(180deg, #f8f5f0 0%, #f2ede6 50%, #ede7dc 100%);">
    <div class="container mx-auto px-4">
      
      <!-- Hero Section -->
      <div class="text-center mb-16">
        <div class="inline-block mb-4">
          <span class="text-6xl">🍿</span>
        </div>
        <h1 class="text-5xl md:text-6xl font-extrabold mb-4 text-gray-900">
          Confitería <span style="color: #e50914;">UVK</span>
        </h1>
        <p class="text-xl text-gray-700 max-w-3xl mx-auto mb-8">
          Complementa tu experiencia cinematográfica con nuestros deliciosos snacks y bebidas. 
          Disponibles durante tu compra de entradas.
        </p>
        <a 
          href="/" 
          class="inline-flex items-center gap-2 bg-red-600 text-white px-8 py-4 rounded-xl font-bold text-lg hover:bg-red-700 transition-all shadow-lg hover:shadow-xl transform hover:scale-105"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 5v2m0 4v2m0 4v2M5 5a2 2 0 00-2 2v3a2 2 0 110 4v3a2 2 0 002 2h14a2 2 0 002-2v-3a2 2 0 110-4V7a2 2 0 00-2-2H5z"></path>
          </svg>
          Comprar Entradas
        </a>
      </div>

      <!-- Info Banner -->
      <div class="bg-gradient-to-r from-red-50 to-orange-50 border-l-4 border-red-600 rounded-lg p-6 mb-12 shadow-md">
        <div class="flex items-start gap-4">
          <svg class="w-6 h-6 text-red-600 flex-shrink-0 mt-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <div>
            <h3 class="font-bold text-lg text-gray-900 mb-1">¿Cómo comprar confitería?</h3>
            <p class="text-gray-700">
              Selecciona tus productos favoritos durante el proceso de compra de entradas. 
              Después de elegir tus asientos, podrás añadir todos los alimentos y bebidas que desees.
            </p>
          </div>
        </div>
      </div>

      <!-- Productos desde la base de datos -->
      {alimentos && alimentos.length > 0 && (
        <div class="mb-16">
          <h2 class="text-3xl font-bold text-gray-900 mb-8 text-center">
            Nuestros Productos
          </h2>
          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
            {alimentos.map((producto) => (
              <div class="bg-white rounded-2xl shadow-lg overflow-hidden transform hover:scale-105 transition-all duration-300 border-t-4 border-red-600">
                {producto.imagen_url && (
                  <div class="h-48 overflow-hidden bg-gray-100">
                    <img 
                      src={producto.imagen_url} 
                      alt={producto.nombre}
                      class="w-full h-full object-cover"
                    />
                  </div>
                )}
                <div class="p-6">
                  <h3 class="text-xl font-bold mb-2 text-gray-900">{producto.nombre}</h3>
                  {producto.descripcion && (
                    <p class="text-gray-600 text-sm mb-4">{producto.descripcion}</p>
                  )}
                  <div class="flex items-center justify-between">
                    <span class="text-2xl font-bold text-red-600">
                      S/ {producto.precio.toFixed(2)}
                    </span>
                    <span class="inline-block px-3 py-1 bg-red-100 text-red-600 rounded-full text-sm font-semibold">
                      Disponible
                    </span>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      )}

      <!-- Promociones Especiales -->
      <div class="mb-16">
        <h2 class="text-3xl font-bold text-gray-900 mb-8 text-center">
          Promociones Especiales
        </h2>
        <div class="grid md:grid-cols-3 gap-6">
          <div class="bg-white rounded-2xl shadow-lg p-8 text-center border-t-4 border-yellow-500">
            <div class="text-5xl mb-4">🎉</div>
            <h3 class="font-bold text-xl mb-3 text-gray-900">Martes de Descuento</h3>
            <p class="text-gray-700 mb-4">20% off en todos los combos</p>
            <div class="inline-block px-4 py-2 bg-yellow-100 text-yellow-800 rounded-full font-semibold text-sm">
              Solo Martes
            </div>
          </div>

          <div class="bg-white rounded-2xl shadow-lg p-8 text-center border-t-4 border-blue-500">
            <div class="text-5xl mb-4">🔄</div>
            <h3 class="font-bold text-xl mb-3 text-gray-900">Recarga Gratis</h3>
            <p class="text-gray-700 mb-4">Recarga tu bebida grande sin costo</p>
            <div class="inline-block px-4 py-2 bg-blue-100 text-blue-800 rounded-full font-semibold text-sm">
              Todo el día
            </div>
          </div>

          <div class="bg-white rounded-2xl shadow-lg p-8 text-center border-t-4 border-green-500">
            <div class="text-5xl mb-4">👥</div>
            <h3 class="font-bold text-xl mb-3 text-gray-900">Combo Familiar</h3>
            <p class="text-gray-700 mb-4">Compra 4+ entradas y obtén descuento en confitería</p>
            <div class="inline-block px-4 py-2 bg-green-100 text-green-800 rounded-full font-semibold text-sm">
              Grupos
            </div>
          </div>
        </div>
      </div>

      <!-- Call to Action Final -->
      <div class="bg-gradient-to-r from-red-600 to-red-700 rounded-2xl shadow-2xl p-12 text-center text-white">
        <h2 class="text-4xl font-bold mb-4">¿Listo para disfrutar?</h2>
        <p class="text-xl mb-8 opacity-90">
          Compra tus entradas ahora y añade tus productos favoritos durante el proceso
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="/" 
            class="inline-flex items-center justify-center gap-2 bg-white text-red-600 px-8 py-4 rounded-xl font-bold text-lg hover:bg-gray-100 transition-all shadow-lg"
          >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 4v16M17 4v16M3 8h4m10 0h4M3 12h18M3 16h4m10 0h4M4 20h16a1 1 0 001-1V5a1 1 0 00-1-1H4a1 1 0 00-1 1v14a1 1 0 001 1z"></path>
            </svg>
            Ver Películas
          </a>
          <a 
            href="/promociones" 
            class="inline-flex items-center justify-center gap-2 bg-transparent border-2 border-white text-white px-8 py-4 rounded-xl font-bold text-lg hover:bg-white hover:text-red-600 transition-all"
          >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v13m0-13V6a2 2 0 112 2h-2zm0 0V5.5A2.5 2.5 0 109.5 8H12zm-7 4h14M5 12a2 2 0 110-4h14a2 2 0 110 4M5 12v7a2 2 0 002 2h10a2 2 0 002-2v-7"></path>
            </svg>
            Ver Promociones
          </a>
        </div>
      </div>

      <!-- Nota Legal -->
      <div class="text-center mt-12 text-gray-600">
        <p class="text-sm">
          * Los precios y promociones pueden variar según ubicación. Consulta disponibilidad al momento de tu compra.
        </p>
        <p class="text-sm mt-2">
          * La confitería solo está disponible durante la compra de entradas.
        </p>
      </div>
    </div>
  </main>
</Layout>

<style>
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  main > div > div {
    animation: fadeIn 0.6s ease-out;
  }
</style>